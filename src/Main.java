public class Main {
    protected static final int NUM_CHAIRS = 3;
    public static void main(String[] args) {
        // Instantiate the SleepingTA class with the number of students input.
        new SleepingTA(5);

        // Exit the system.
        System.exit(0);
    }
}
